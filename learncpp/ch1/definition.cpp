int main()
{
    int x; // definition of variable x
    
    // correct (but not recommended)
    int y, z; // definition of variable x
    
    // int a, double b; // wrong (compiler error)

    int c; double d; // correct (but not recommended)

    // correct and recommended (easier to read)
    int e;
    double f;

    return 0;
}
